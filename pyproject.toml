[project]
name = "pomvsprocedural"
version = "0.1.0"
description = "Compare POM (Page Object Model) and Procedural Design Pattern Automation Script"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest-playwright>=0.8.0",
]
